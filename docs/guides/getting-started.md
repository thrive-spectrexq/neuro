# Getting Started

## Prerequisites
- Node.js (v20+)
- Python (3.12+)
- pnpm (v9+)
- Redis (Optional but recommended)

## Setup
1. Run `pnpm install`
2. Run `scripts/setup.ps1` or `scripts/setup.sh`
3. Run `scripts/dev.ps1` or `scripts/dev.sh`
